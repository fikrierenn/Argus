# Oturum Günlüğü --- 2026-08-26


## Compact Snapshot --- 14:11

### Son 5 Commit

- 15f26d1 docs(rules): arac kullaniminda token disiplini — ajan promptu Ingilizce, cikti suzulur
- 6bf58a9 feat(risk): Risk gezgini tasindi + SAYFALAMA BUG'I duzeltildi (plan: 05, Dalga 1 tamam)
- ce7038b feat(audit): denetim listesi tasindi — <solum-field> ilk gercek kullanim (plan: 05, Dalga 1)
- 194c309 feat(dof): pano yeniden yazildi — CSP temiz, KLAVYE ile tasima, sayfa yenilemesi yok (plan: 05)
- 63373e4 feat(web): Korelasyon tasindi, DOF panosunun KPI/baslik kismi tasindi (plan: 05, Dalga 1)

### Uncommitted Dosya Sayısı

- 6 uncommitted dosya

### Aktif Planlar

- 01-audit-log.md
- 02-sema-uzlastirma.md
- 03-argus-uzman-ajanlar.md
- 04-solum-dashboard.md
- 05-solum-tam-tasima.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **A1. `audit.AuditLog` hiç yazılmıyor** — `security-principles.md` denetim izi zorunlu kılıyor.
- - [ ] **A2. `ai.LlmResults` boş** — LLM cevapları kalıcı değil, maliyet ve tekrar-kullanım izi yok.
- - [ ] **A3. `ai.AnalysisQueue` hiç kullanılmamış** — kuyruk mimarisi var, akış `SkillExecutions` üzerinden gidiyor. Karar: kuyruğu bağla **veya** tabloyu emekliye ayır. İkisi arası belirsizlik en kötüsü.
- - [ ] **A4. `ai.Feedback` boş** — geri bildirim UI'ı var, tek kayıt yok. Uçtan uca test edilmemiş.
- - [ ] **A5. Boşta duran 6 AI SP'sini bağla** — `ai.sp_Insight_Insert/Action/Dashboard`, `ai.sp_Feedback_Stats`, `ai.sp_AiDashboard_FeedbackTrend`, `ai.sp_Trigger_PostRiskEtl`. Yazılmış ama çağıran yok.
- - [ ] **A6. Semantik vektör sync hiç üretmemiş** — kod tam (`AiWorkerService.cs:181-215`, `VectorSyncMinutes: 60`) ama `ai.SemanticVectors` **0 satır**. Kaynak SP (`ai.sp_SemanticVector_SourceList`) boş mu dönüyor, yoksa Ollama embedding mi patlıyor? *(Eski TODO "Semantik hafıza vektör sync" — kod tarafı kapalı, veri tarafı açık.)*
- - [ ] **A7. LLM kuyruğu hiç işlememiş** — `LLM_QUEUED` geçişi ve işleyici var (`AiWorkerService.cs:154,429`) ama `ai.LlmResults` **0 satır**. A2 ile aynı kök. *(Eski TODO "LLM queue işleme".)*
- - [ ] **B4. `sem.vw_Stale` curator akışı** — `session-handoff` sırasında 7 günde bir bayat kayıt taraması.
- - [ ] **B11. Risk sunum eşiklerini `ref.RiskParameters`'tan oku** — `RiskView.cs` 90/70 sunum eşiği taşıyor; semantik katmanda `KritikSkorEsik` tanımı var (`sql/47:91`). Danışman kuralı "eşiği kodda sabitleme". Skor hesabına DOKUNULMAZ, yalnız rozet rengi.
- - [ ] **B12. `<solum-field>` çoklu seçim türü yok** — `Risk/Index`'te mekan/tip onay kutusu grupları elle yazılı (10 kutu). Solum'a bildirildi, üç-ürün eşiğine takılıyor; Solum almazsa `argus-checkgroup` kalıcı olur.
- - [ ] **B5. Skill yönetim ekranı** — `ai.Skills`/`SkillVersions` için Razor sayfası (prompt görüntüle, sürüm geçmişi, aktif/pasif). Şu an sadece SQL'den yönetilebiliyor. `Policies.AdminOnly`.
- - [ ] **C1. `Features/Ref/Index` böl** — 1214 satır PageModel + 1145 satır Razor, 8 sekme tek dosyada. Sekme başına partial + `RefService`. `csharp-conventions.md` 500 satır kırmızı çizgisi.
- - [ ] **C2. `LlmService.cs` (1129) ve `AiWorkerService.cs` (936) böl** — sağlayıcı başına dosya.
- - [ ] **C3. 62 build uyarısını sıfırla** — çoğu nullable (CS8618/8601/8603). Doğru çözüm `required`/`init`, pragma değil.
- - [ ] **C4. Ölü dosyaları kaldır** — `TestDebug.cs` (ikinci `Main`, CS7022), `DbTest.cs`, `DebugTest.cs`, `TestEmbedding.cs` prod binary'sine giriyor.



---

# Dalga 1 Kapanış Denetimi ve Bulgu Kapatma (akşam)

## Ne oldu

Plan 05 Dalga 1 (altı ekran Solum ilkellerine taşındı) kapanış denetimine girdi.
Üç denetçi paralel koştu: `code-reviewer` (sonnet), `security-reviewer` (opus),
`silent-failure-hunter` (opus). Toplam **1 CRITICAL yalan, 1 CRITICAL sessiz
kırpma, 8 HIGH** ve bir dizi MEDIUM çıktı. Denetçilerin dosyaları okuduğu süre
boyunca `Features/` ve `wwwroot/js` altında **hiç düzenleme yapılmadı** (kullanıcı
talimatı: "diğer ikisini bekle, gelince hepsini düzelt").

Bulgular ikiye bölündü:
- **Sunum + sessiz hata** (C#/Razor/JS) → bugün kapatıldı, Tier 2.
- **Sunucu kapısı + şema** (SP/yetki) → `plans/06-denetci-bulgulari-sunucu.md`, Tier 3.

## Bugün kapatılanlar (ölçümle)

| # | Bulgu | Ne yapıldı | Kanıt |
|---|---|---|---|
| 1.1 | **CRITICAL** Excel indir sessizce 200 satırda kesiliyordu (`PageSize=5000` gönderiliyordu ama SP `IF @PageSize > 200` ile kırpıyor; `@Top` `COALESCE` yüzünden inert) | Sayfa döngüsü + kırpma dosya adında | `risk_listesi_20260826_ILK5000_KIRPILDI.xlsx` (28 sn, 25 SP çağrısı) |
| 5.1 | **CRITICAL** Ürün DÖF sekmesi "bu ürün için açılmış düzeltici faaliyet bulunmuyor" diyordu; `Doflar` hiçbir yerde doldurulmuyor → mükerrer DÖF riski | Ekran artık iddiada bulunmuyor; veri yolu plan 06 | `?tab=dof` → yeni metin var, eski yanlış metin yok |
| 2.2 | **HIGH** `try` bloğu DOM işlemlerini de sarıyordu → başarılı geçiş "Bağlantı hatası. Durum değişmedi." diye raporlanıyordu | `try` yalnız fetch+json; DOM hatası artık "durum değişti, ekran güncellenemedi" der | kod |
| 2.3 | **HIGH** İki hızlı Alt+→ aynı geçişi iki kez POST ediyordu, ikinci adım kayboluyordu | Kart üzerinde `data-argus-busy` kilidi | kod |
| 2.5 | **HIGH** SP'nin gerçek reddi ("Invalid transition ... role: DENETCI") jenerikle eziliyordu | `SqlException 50000-59999` yakalanıyor, iki bilinen kalıp Türkçeye eşleniyor, tanınmayan mesaj sızmıyor | kod |
| 3.1 | **HIGH** Pano en yeni 100 bulgu; gecikmişler tanım gereği eski olduğu için kırpılan dilime düşüyordu | Kırpma uyarısı + KPI toplamı yanında | `Kirpildi` |
| 3.2 | **HIGH** Taşımadan sonra KPI bandı sunucu gerçeğinden ayrışıyordu (reload kaldırılmıştı) | `data-argus-summary-stale` bandı, JS başarılı taşımada açıyor | işaret sayfada ölçüldü |
| 3.3 | **HIGH** KPI satırı yoksa "SLA geciken **0**" YEŞİL basılıyordu — veri yokluğu iyi haber gibi | `KpiVar` yanlışsa değer "—", ton nötr | kod |
| 5.2 | **HIGH** Kayıt yoksa uydurma kimlik (`BK-88123`, `Urun-88123`, `Donem="Son30Gun"`) + tek uyarı AI özetiyle siliniyordu | Uydurma yok, banda taşındı, uydurma dönemle AI sorgusu koşmuyor, `id<=0` → 404 | `BK-88123` yok, banda var, `id=0` → HTTP 404 |
| 5.5 | **HIGH** Denetim listesi en yeni 100 kaydı "N kayıt" diye toplam gibi gösteriyordu | Kırpma uyarısı + başlık notu | kod |
| 6.1 | Stok `decimal(18,3)` ama `"N0"` ile yuvarlanıyordu (0,4→"0", 2,5→"3") — denetçi ERP dökümüyle kıyaslayınca **uydurma bulgu** üretiyordu | `ArgusFormat.Quantity` ondalığı korur (4 yer) | kod |
| 1.2 | Kesim "başlangıç/bitiş" aralık değil, aralığın son günü | Ekran gerçeği yazıyor; SQL kararı plan 06 | "en son kesim günü" ipucu sayfada |
| 1.3 | Seçenekte olmayan süzgeç değeri görünmez süzgeç üretiyordu (Korelasyon → `/Risk?mekan={id}` yolu) | Uyarı bandı; değer atılmıyor, görünür oluyor | `?mekan=999999` → band var |
| 1.4 | "Dönem" kolonu seçim varmış gibi duruyordu, SP tek dönemi sabitliyor | Kolon kaldırıldı, kart altbaşlığına taşındı | kod |
| 1.6 | Çoklu seçimde indeks boşluğu bütün diziyi düşürebiliyordu | Boşlar indekslemeden ÖNCE ayıklanıyor | kod |
| 2.4 | 401/403 "geçiş yapılamadı" gibi görünüyordu | Oturum mesajı ayrı | kod |
| 2.6 | `clearTimeout` yalnız `tur` doluyken çağrılıyordu → "işleniyor" yazısı iş uçarken siliniyordu | Koşulsuz | kod |
| 2.7 | Eksik `data-*` niteliğinde tamamen sessiz dönüş | Türkçe mesaj | kod |
| 3.4 | Boş sayfa ile "kayıt yok" aynı mesajı veriyordu, kullanıcı sağlam süzgecini bozuyordu | Ayrı metin | `?sayfa=99999` → "Bu sayfada kayıt yok" + "Veri bitti" |
| 3.5 | REJECTED, "Kapandı" kolonunda kapanmış gibi duruyordu | Kart üzerinde durum rozeti | kod |
| 4.1 | Bildirim okundu fetch'i yanıta HİÇ bakmıyordu; `<a href>` gezinmesi isteği iptal edebiliyordu | `keepalive: true` + `.then/.catch` + toast; 0 satır → 404 + log | kod |
| 4.2 | Toast zamanlayıcısız (sayfa ömrü boyunca kalıyordu) ve yalnız DÖF sayfasında tanımlıydı → diğer ekranlarda `alert()` | Zamanlayıcı + `_Layout`'a taşındı | kabukta ölçüldü |
| 5.3 | "Yeniden hesapla" hiçbir şey söylemiyordu; boş sonuç aynı ipucunu tekrar veriyordu (sonsuz döngü) | Ölçülen satır sayısıyla mesaj + `try/catch` + `LogError` + boş durum ayrımı | kod |
| 5.4 | Korelasyon "Toplam mekan" 100'de sabit, ortalama süzgece göre kayıyordu | Etiketler koşullu ("Listelenen mekan", "(süzgeçli)") | kod |
| 5.7 | Anasayfa KPI'sı sessizce yok olabiliyordu; bilinmeyen ton **kırmızı** oluyordu (veri kaybı alarm kılığında) | Boş durum + `tone-alert` açık eşleme + bilinmeyen → nötr | kod |
| 6.2 | Skor 0 "ORTA" etiketleniyordu, "DÜŞÜK" hiç üretilemiyordu | Eşikler tamamlandı (≥50 ORTA, >0 DÜŞÜK, 0 YOK) | kod |
| 6.3 | Denetim bitiş tarihi gece yarısıydı → aynı günün denetimleri düşüyordu | C# gün sonuna çekiyor; SP düzeltmesi plan 06 | kod |
| 6.4 | Eşik 0 ile "eşik tanımsız" aynı gösteriliyordu | `decimal?` + null kontrolü | kod |

Kural-uyum bulguları: `ArgusFormat` + `ArgusBadge` (beş dosyadaki `TrCulture`
kopyası ve 25+ elle yazılmış rozet dizesi tek yere indi), `Domain/DofStatus`
sabit sınıfı (`Dof/Index` bağlandı; `Detail` Dalga 2'de).

## Doğrulanan bir "DOĞRULANMADI"

Sessiz-hata denetçisi `RiskView.RouteData`'nın ürettiği `mekan[0]` anahtarının
gerçekten bağlandığını ölçmediğini söyledi. **Ölçüldü:** `?mekan=1` ve
`?mekan[0]=1` ikisi de bağlanıyor (onay kutusu `checked`, 50 satır); `?mekan=999999`
0 satır + uyarı bandı. Sayfalama da gerçekten sayfalıyor — sayfa 1/2/3/99/200 ilk
satırları farklı ürünler.

## Solum tarafı

`.solum-alert-warn` eksikliği bildirildi (ölçüm: `solum.css:403-405`). Solum aileyi
tarayıp **üç** boşluk buldu (`alert-neutral`, `alert-warn`, `badge-neutral`) ve
kök sebebi yazdı: `SolumHtml.ToneClass(tone, prefix)` herhangi bir ön-ek aldığı
için C# var olmayan sınıf adı üretebiliyordu. Geçici `argus-alert-warn`
kaldırıldı, `solum-alert-warn` kullanılıyor.

Solum görsel doğrulama istedi. **Kısmen yapılabildi:** BkmArgus kabuğu
`<html data-solum-theme="light">` ile temayı sabitliyor, yani koyu tema bu
uygulamadan doğrulanamaz. Açık temada ölçülen kontrast oranları (WCAG):

| Sınıf | Ön/zemin | Oran | AA (4.5) |
|---|---|---|---|
| `solum-alert-neutral` | #16201c / şeffaf→beyaz | 16,70 | geçer |
| `solum-alert-bad` | #e30613 / #f6e4e0 | 3,97 | geçmez |
| `solum-badge-neutral` | #6e7c75 / #eff2f0 | 3,88 | geçmez |
| `solum-alert-warn` | #a67a17 / #f7efd9 | 3,38 | geçmez |
| `solum-alert-good` | #10b981 / #e4efe9 | **2,15** | büyük metin eşiğini (3,0) bile geçmez |

## Durum

- Build: **0 hata**
- Test: **64/64 geçti**, 0 başarısız
- Smoke: Risk (süzgeç/sayfalama/dışa aktarım/boş sayfa), DÖF panosu, Denetim,
  Ürün (geçersiz id → 404 ve banda), Korelasyon, Anasayfa — hepsi HTTP 200 ve
  beklenen metinler ölçüldü

## Yarına

1. Plan 06 Faz 1: `audit.sp_AuditLog_Write` + `Services/AuditTrail.cs` (domain kararı beklemiyor)
2. Plan 06 Faz 3 öncesi `denetim-surec-danismani`: "DÖF'ü kim taşıyabilir" kararı
3. Plan 05 Dalga 2 (Ai, Ref, Yonetim, Ayarlar, Dof/Detail)
