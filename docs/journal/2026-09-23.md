# Oturum Günlüğü --- 2026-09-23


## Compact Snapshot --- 09:21

### Son 5 Commit

- 91b983e feat(mobil): tablo KART duzeni + OLU satir tiklamasi gercek baglantiya cevrildi (plan: 07, Faz 2)
- f546ece feat(mobil): kabuk ve ust cubuk telefonda sigiyor (plan: 07, Faz 1)
- e54c946 fix(security): bildirim adresi denetimi servise tasindi, TryCreate ile SEBEP de loglaniyor
- 9321e96 fix(solum): 26 gunluk aradan sonra DORT kirici degisiklige uyarlama
- fafb430 fix(security,audit): denetci bulgulari kapatildi — CSRF besinci uc nokta, iz artik ATLATILAMIYOR (plan: 06)

### Uncommitted Dosya Sayısı

- 1 uncommitted dosya

### Aktif Planlar

- 01-audit-log.md
- 02-sema-uzlastirma.md
- 03-argus-uzman-ajanlar.md
- 04-solum-dashboard.md
- 05-solum-tam-tasima.md
- 06-denetci-bulgulari-sunucu.md
- 07-mobil-pwa.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **A1. `audit.AuditLog` hiç yazılmıyor** *(plan 06 I4 olarak ele alınıyor)* — `security-principles.md` denetim izi zorunlu kılıyor.
- - [ ] **A2. `ai.LlmResults` boş** — LLM cevapları kalıcı değil, maliyet ve tekrar-kullanım izi yok.
- - [ ] **A3. `ai.AnalysisQueue` hiç kullanılmamış** — kuyruk mimarisi var, akış `SkillExecutions` üzerinden gidiyor. Karar: kuyruğu bağla **veya** tabloyu emekliye ayır. İkisi arası belirsizlik en kötüsü.
- - [ ] **A4. `ai.Feedback` boş** — geri bildirim UI'ı var, tek kayıt yok. Uçtan uca test edilmemiş.
- - [ ] **A5. Boşta duran 6 AI SP'sini bağla** — `ai.sp_Insight_Insert/Action/Dashboard`, `ai.sp_Feedback_Stats`, `ai.sp_AiDashboard_FeedbackTrend`, `ai.sp_Trigger_PostRiskEtl`. Yazılmış ama çağıran yok.
- - [ ] **A6. Semantik vektör sync hiç üretmemiş** — kod tam (`AiWorkerService.cs:181-215`, `VectorSyncMinutes: 60`) ama `ai.SemanticVectors` **0 satır**. Kaynak SP (`ai.sp_SemanticVector_SourceList`) boş mu dönüyor, yoksa Ollama embedding mi patlıyor? *(Eski TODO "Semantik hafıza vektör sync" — kod tarafı kapalı, veri tarafı açık.)*
- - [ ] **A7. LLM kuyruğu hiç işlememiş** — `LLM_QUEUED` geçişi ve işleyici var (`AiWorkerService.cs:154,429`) ama `ai.LlmResults` **0 satır**. A2 ile aynı kök. *(Eski TODO "LLM queue işleme".)*
- - [ ] **B4. `sem.vw_Stale` curator akışı** — `session-handoff` sırasında 7 günde bir bayat kayıt taraması.
- - [ ] **B11. Risk sunum eşiklerini `ref.RiskParameters`'tan oku** — `RiskView.cs` 90/70 sunum eşiği taşıyor; semantik katmanda `KritikSkorEsik` tanımı var (`sql/47:91`). Danışman kuralı "eşiği kodda sabitleme". Skor hesabına DOKUNULMAZ, yalnız rozet rengi.
- - [ ] **B12. `<solum-field>` çoklu seçim türü yok** — `Risk/Index`'te mekan/tip onay kutusu grupları elle yazılı (10 kutu). Solum'a bildirildi, üç-ürün eşiğine takılıyor; Solum almazsa `argus-checkgroup` kalıcı olur.
- - [ ] **B5. Skill yönetim ekranı** — `ai.Skills`/`SkillVersions` için Razor sayfası (prompt görüntüle, sürüm geçmişi, aktif/pasif). Şu an sadece SQL'den yönetilebiliyor. `Policies.AdminOnly`.
- - [ ] **C1. `Features/Ref/Index` böl** — 1214 satır PageModel + 1145 satır Razor, 8 sekme tek dosyada. Sekme başına partial + `RefService`. `csharp-conventions.md` 500 satır kırmızı çizgisi.
- - [ ] **C2. `LlmService.cs` (1129) ve `AiWorkerService.cs` (936) böl** — sağlayıcı başına dosya.
- - [ ] **C3. 62 build uyarısını sıfırla** — çoğu nullable (CS8618/8601/8603). Doğru çözüm `required`/`init`, pragma değil.
- - [ ] **C4. Ölü dosyaları kaldır** — `TestDebug.cs` (ikinci `Main`, CS7022), `DbTest.cs`, `DebugTest.cs`, `TestEmbedding.cs` prod binary'sine giriyor.


---

## Ana Konu

Plan 07 (tam mobil + kurulabilir PWA) Faz 2'nin kapanışı ve **Solum'a resmî
talep listesi** (T0–T6). Oturumun asıl çıktısı kod değil, karşı-repoya giden
ölçülmüş talep dosyasıdır: mobil işin kalan üç fazından ikisi Solum'un
cevabına bağlı.

## Tamamlananlar

- **Solum'a mobil devir mesajı gönderildi** (oturumlar arası mesaj,
  hedef: `Solum Framework geliştirme [c6caa1]`, 2026-09-23). İçeriği bu
  günlüğün companion dosyasında **tam** duruyor:
  `docs/journal/2026-09-23-kalan-isler.md`.
- Talep içeriği ölçümle doğrulandı (tahmin yok):
  - `solum.js:44` → `document.querySelector("[data-solum-shell]")` (5. kırıcı değişiklik teyidi)
  - `TableRenderer.cs:278` → düz `<td>`, `data-solum-label` deposunda **0 eşleşme**
  - `solum.css` içinde `pointer: coarse` sorgusu **0**; `.solum-btn` dolgusu `7px 13px`
  - `.solum-row-side` `flex-shrink: 0`, sarma yok; `.solum-card-head` `nowrap`
  - Solum kırılma noktaları: 1100 / 860 / 600 (üç tane, `solum.css:588,1064,593`)

## Build / Test Durumu

- Build: **yeşil** — 0 hata, **1 uyarı**
  (`Features/Dof/Detail.cshtml:39` CS8321, kullanılmayan `StatusLabel` yerel
  fonksiyonu — Faz 3'te o dosyaya zaten dokunulacak, orada temizlenecek)
- Test: bu oturumda **çalıştırılmadı** (kod değişmedi; son bilinen 64/64)
- Smoke: bu oturumda **çalıştırılmadı** (kod değişmedi)

## Commit Durumu

- Bu oturumda üretim kodu commit'i **yok** — kod değişmedi
- Uncommitted: yalnız bu günlük + companion
- Son commit'ler 2026-09-22 tarihli (91b983e Faz 2, f546ece Faz 1)

## Aktif Planlar

- `plans/07-mobil-pwa.md` — **Faz 1 ✅, Faz 2 ✅**, Faz 3/4 **Solum'a bağlı (T5)**, Faz 5/6 serbest
- `plans/06-denetci-bulgulari-sunucu.md` — Faz 1/2/4/5 kısmen kapandı; Faz 3 domain kararı bekliyor
- `plans/01,02,03` — 14+ gün dokunulmadı, `plan-first.md` gereği ısıt veya arşivle (karar verilmedi)

## Yarım Kalan / Yarın'a Bırakılan İşler

**Tamamı companion dosyada, NE · NEDEN · NEREDE · NASIL KAPANIR · BAĞIMLILIK ·
ÖNCELİK şemasıyla:** `docs/journal/2026-09-23-kalan-isler.md`.
Başlıklar: Solum T0–T6 cevabı · Faz 3 DÖF dokunmatik · Faz 4 süzgeç ·
Faz 5 PWA · Faz 6 ölçüm · plan 06 artıkları · fresh-DB testinin tekrarı.

## İşe YARAMAYANLAR

- **`getBoundingClientRect().width` ile "menü görünür mü" ölçmek** → yanlış.
  `translateX(-100%)` ile ekran dışına itilmiş yan menü de **256** döndürüyor.
  Doğru ölçüt `left`/`right`: ölçülen `left=-256, right=0` → menü kapalı.
  Bu yanlış ölçüm yüzünden Solum'un mobil kabuğunu bir süre "bozuk" sandık;
  değilmiş. Ekran dışılık **konumdan** okunur, genişlikten değil.
- **Kart düzenini yalnız tabloyu daraltarak çözmek** → yetmedi. Tablo 673px'ten
  327px'e indi ama sayfa hâlâ 137px taşıyordu; suçlu tablo değil çevresindeki
  `.solum-row-side` idi. Ders: taşmayı **taşan öğeyi ölçerek** bul, en büyük
  öğeyi varsayarak değil.

## Kararlar

- **Solum'dan istenen 7 madde (T0–T6) ve her birinin "alınmazsa bizde kalan"
  karşılığı yazıldı.** Reddedilmesi de geçerli cevap sayılıyor; üç-ürün eşiği
  bizim değil onların kuralı.
- **T3 ve T4'ün Solum'a ait olduğu açıkça beyan edildi:** `argus-theme.css`
  şu an onların sınıflarını gölgeliyor (`.solum-btn`, `.solum-row*`). Bu bir
  tema tercihi değil mekanizma boşluğu; gelirse bloklar silinecek.
- **Faz 3/4 Solum cevabı gelmeden başlatılmıyor.** İkisi de alt sayfa
  (bottom sheet) istiyor; `argus-sheet`'i yazıp sonra onlarınkine geçmek
  iki kez iş demek.
- **Faz 5 (PWA) Solum yüzeyine hiç dokunmuyor** → beklemeden yapılabilir.

## Dikkat Edilmesi Gerekenler

- **BAŞKA MAKİNEDE ÇALIŞILACAK.** `src/BkmArgus.Web/BkmArgus.Web.csproj:23-25`
  üç `ProjectReference` ile **`D:\Dev\solum` canlı çalışma ağacına** bağlı
  (`..\..\..\solum\src\...`). O depo aynı göreli konumda yoksa **build hiç
  başlamaz**. Bu kopukluk TODO C9 olarak kayıtlı (NuGet'e geçiş borcu).
- Solum `master` başı: `565d7a2` (2026-09-23 09:22). Bizim uyarlamalarımız
  `9321e96`'ya kadar olan hali karşılıyor; arada yeni kırıcı değişiklik
  olabilir, **oturuma build ile başla**.
- Solum tüketici taleplerini `docs/ISTEK-<TUKETICI>-<TARIH>.md` olarak
  yazıyor (örn. `ISTEK-MAGAZA-2026-09-22.md`). Bizim cevabımız muhtemelen
  `ISTEK-ARGUS-2026-09-23.md` adıyla oraya düşecek — **önce oraya bak**.
- Branch: **`feature/solum-kabuk`** (master değil).

---

## EK — Solum'un cevabı AYNI GÜN geldi

Talep gönderildikten kısa süre sonra cevap ulaştı. **Altı talebin altısı da
onların deposunda ölçüldü ve altı iddia da doğrulandı**, ama **hiçbiri bugün
uygulanmıyor** (aktif başlıkları K4 / `plans/26`). Tam özet companion'da §0b.

Bizi doğrudan etkileyen dört sonuç:

1. **T5 kapandı, Faz 3'ün bağımlılığı kalktı.** `solum-sheet` | `bottom-sheet`
   → 0, planlarında da yok. `argus-sheet`'i **biz yazıyoruz**. Faz 3 artık
   beklemiyor.
2. **🔴 `Solum.Core` + `Solum.EntityFrameworkCore`'da kırıcı değişiklik
   duyuruldu, kod henüz yazılmamış.** Geldiği gün **altıncı uyarlama turu**.
   EF kullanmıyoruz ama `Solum.Core`'a `ProjectReference` ile bağlıyız.
3. **T2 bizim istediğimizden DAR bir öneriye döndü ve kabul edilebilir:**
   yalnız `cursor:pointer` kalkacak (`TableRenderer.cs:262`), `data-solum-href`
   kancası kalacak. Gerekçe ölçümle çürüdü — CHANGELOG'da "bir tüketici kendi
   işleyicisiyle kullanıyor" yazıyormuş; ölçtüklerinde o kancanın tek kullanımı
   **bizim CSS kuralımız** (`argus-theme.css:435`), işleyici değil.
   Bizde **hiç iş çıkmıyor**, çünkü kendi cursor'umuzu zaten basıyoruz.
   `[Obsolete]` önerilerini reddettiler (kendi plan 23 §9-S1'i bir kez
   reddetmiş: kapıyı silmiyor, yüzeyi büyütüyor) — bu değerlendirme bizim için
   de geçerli, istediğimiz sonuç birinci maddeyle geliyor.
4. **T6 alınmadı** (1/3 eşiği) → `argus-checkgroup` bizde **kalıcı**.
   TODO B12 bu sonuca göre güncellenmeli.

Ayrıca **ölçümümüz onlarda bir kusur açığa çıkardı**: `solum-board*.js`
olaylarında `touchstart` ve `pointerdown` **0** — Solum'un kendi panosu da
telefonda kullanılamıyor. Kendi `js-disiplini.md §5`'i "fare olmadan uçtan
uca" derken yalnız klavyeyi zorunlu kılıyormuş, dokunmayı fare sayıyormuş.
Kural boşluğu olarak kaydettiler.

**T0'ın çıktısı:** kırıcı değişikliğin ikiye ayrıldığını kabul ettiler —
**derlemeyi kıran** ve **davranışı kıran**. Bizim beşinci vakamız (imza aynı,
derleme yeşil, davranış ölü) ikinci sınıf ve bugün onun için **hiçbir duyuru
yolu yok**.

## Yarına Başlangıç Noktası

1. **Build al.** Solum `master` `565d7a2`'ye ilerledi + `Solum.Core` kırıcı
   değişikliği duyuruldu. `D:\Dev\solum` aynı göreli konumda olmalı.
2. **Plan 07 Faz 5 — PWA.** Solum'a hiç bağlı değil; onların K4 işi bitmeden
   T1–T4 zaten gelmeyecek.
3. **Plan 07 Faz 3 — DÖF dokunmatik.** T5 cevabı geldi, bağımlılık yok.
